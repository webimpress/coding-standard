<?php

namespace A {
    use ArrayObject as AO;

    new \DateTime;

    new AO;
    new AO;
    new AO();

    \DateTime::createFromFormat();
}

namespace B {
    use ArrayObject;
    use DateTime as DT;

    use const T_NAMESPACE;
    use function min;

    new DT;
    new DT;
    new DT();

    DT::createFromFormat();

    new ArrayObject();
    min(T_NAMESPACE, 10);
}
