<?php

namespace MyNamespace;

use function array_keys;
use function count;
use function range;

$arr = range(1, 10);
$count = count($arr);

$keys = array_keys($arr);
$count = count($keys);

$val = myFunction($arr);
